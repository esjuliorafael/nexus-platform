name: CI/CD Nexus Platform

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build & Push Nexus API
      - name: Build and push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexus-api:latest
          cache-from: type=gha
          cache-to: type=inline

      # Build & Push Nexus Storefront (Generic Image)
      - name: Build and push Storefront
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/storefront/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexus-storefront:latest
          cache-from: type=gha
          cache-to: type=inline

      # Build & Push Nexus Admin (Generic Image)
      - name: Build and push Admin
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/admin/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexus-admin:latest
          build-args: |
            VITE_RAFFLE_ENABLED=true
          cache-from: type=gha
          cache-to: type=inline
